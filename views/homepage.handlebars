{{#each posts as |post|}}
<div class="row mb-4">
    <div class="col-md-5">
        <a href="/post/{{post.id}}">{{post.title}}</a>
        <p>Posted by: {{post.user.username}} on {{post.created_at}}</p>
    </div>
</div>
{{/each}}